# `TaskSpec` model reference

`TaskSpec` identifies what to run - the fully-qualified function name (`module:function`) and its args and kwargs. `TaskSpec` is part of `Task`.

::: sheppy.models.TaskSpec

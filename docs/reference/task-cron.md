# `TaskCron` model reference

`TaskCron` is a registered cron job: a cron expression plus the task to instantiate on each run. Instances are created by [`Queue.add_cron()`](queue.md); see the [Cron jobs](../guides/cron.md) guide.

::: sheppy.models.TaskCron
